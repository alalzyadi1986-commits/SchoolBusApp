# دليل هندسة مشروع SchoolBusApp 🚌

## 1. مقدمة

هذه الوثيقة هي دليل شامل لمشروع **SchoolBusApp**، مصممة لتوفير فهم عميق لهيكل التطبيق، تدفق البيانات، والمنطق البرمجي. تهدف هذه الوثيقة إلى أن تكون مرجعاً أساسياً لأي مبرمج أو نظام ذكاء اصطناعي يرغب في فهم المشروع، تطويره، أو صيانته دون الحاجة لشرح إضافي.

**SchoolBusApp** هو تطبيق React Native متكامل يهدف إلى إدارة وتتبع حافلات المدارس، مع أدوار متعددة للمستخدمين (المدير العام، مدير المدرسة، السائق، المرافقة، ولي الأمر). يعتمد التطبيق بشكل أساسي على **Firebase Realtime Database** لتخزين البيانات و **Expo** لإدارة جوانب مثل الموقع والإشعارات.

## 2. البنية المعمارية وهيكلة الملفات 🏗️

يعتمد المشروع على بنية معيارية (Modular Architecture) حيث يتم تقسيم التطبيق إلى وحدات مستقلة بناءً على وظائفها وأدوار المستخدمين. هذا يسهل عملية التطوير والصيانة.

### 2.1. المخططات الهيكلية

تم إعداد مخططين لتوضيح البنية العامة والتفصيلية للمشروع، وهما مرفقان مع هذه الوثيقة:

*   **`project_diagram.png`**: يوضح تدفق البيانات والتبعيات بين جميع الملفات والخدمات في المشروع.
*   **`project_structure.png`**: يقدم نظرة عامة هرمية على بنية التطبيق، موضحاً كيفية ارتباط الشاشات المختلفة بالخدمات وقاعدة البيانات.

### 2.2. هيكل المجلدات والملفات الرئيسية

```
SchoolBusApp/
├── .expo/                                  # إعدادات Expo
├── assets/                                 # الأصول (صور، أيقونات)
├── docs/                                   # وثائق المشروع (مثل LostAndFound_Firebase_Schema.md)
├── src/                                    # الكود المصدري الرئيسي
│   ├── firebaseConfig.js                   # إعدادات Firebase (الاتصال بقاعدة البيانات والتخزين)
│   ├── hooks/                              # React Hooks مخصصة
│   │   └── useFirebase.js                  # Hook لجلب البيانات من Firebase (غير مستخدم حالياً)
│   ├── i18n.js                             # ملف الترجمة (عربي/إنجليزي)
│   ├── modules/                            # الوحدات الرئيسية للتطبيق (لكل دور مستخدم أو وظيفة)
│   │   ├── admin/                          # وحدة المدير العام (Super Admin)
│   │   │   ├── screens/
│   │   │   │   └── SuperAdminScreen.js     # شاشة إدارة المدارس والمستخدمين
│   │   ├── auth/                           # وحدة المصادقة (Authentication)
│   │   │   ├── screens/
│   │   │   │   └── LoginScreen.js          # شاشة تسجيل الدخول
│   │   │   └── services/
│   │   │       └── authService.js          # خدمة منطق تسجيل الدخول والتحقق من الصلاحيات
│   │   ├── driver/                         # وحدة السائق
│   │   │   ├── components/
│   │   │   │   ├── DriverHeader.js         # رأس شاشة السائق
│   │   │   │   ├── DriverMap.js            # مكون الخريطة للسائق
│   │   │   │   ├── SpeedCard.js            # بطاقة عرض السرعة
│   │   │   │   ├── StudentCard.js          # بطاقة الطالب (مكررة؟)
│   │   │   │   ├── StudentItem.js          # عنصر الطالب في القائمة
│   │   │   │   └── StudentList.js          # قائمة الطلاب (مكررة؟)
│   │   │   ├── screens/
│   │   │   │   └── DriverScreen.js         # الشاشة الرئيسية للسائق (تتبع، طلاب، مفقودات)
│   │   │   └── services/
│   │   │       ├── driverBackgroundService.js # خدمة تتبع الموقع في الخلفية
│   │   │       ├── driverLocationService.js   # خدمة تتبع الموقع المباشر (Foreground)
│   │   │       ├── driverStudentService.js    # خدمة جلب طلاب السائق
│   │   │       └── driverTripService.js       # خدمة بدء/إنهاء الرحلة (غير مستخدمة حالياً)
│   │   ├── lostAndFound/                   # وحدة المفقودات والمعثورات
│   │   │   ├── components/
│   │   │   │   └── LostAndFoundComponents.js # مكونات عرض وإضافة المفقودات
│   │   │   └── services/
│   │   │       ├── lostAndFoundService.js    # خدمة إدارة المفقودات (إضافة، حل، بحث)
│   │   │       └── notificationService.js    # خدمة إشعارات المفقودات
│   │   ├── parent/                         # وحدة ولي الأمر
│   │   │   ├── components/
│   │   │   │   └── ParentMap.js            # مكون الخريطة لولي الأمر
│   │   │   ├── screens/
│   │   │   │   └── ParentScreen.js         # الشاشة الرئيسية لولي الأمر (تتبع، حالة الطالب، مفقودات)
│   │   │   └── services/
│   │   │       ├── parentAlertService.js     # خدمة تنبيهات وصول الباص
│   │   │       ├── parentBusService.js       # خدمة جلب معلومات الباص والسائق
│   │   │       ├── parentLocationService.js  # خدمة جلب موقع ولي الأمر
│   │   │       ├── parentSchoolService.js    # خدمة جلب معلومات المدرسة
│   │   │       └── parentStudentService.js   # خدمة جلب معلومات الطالب لولي الأمر
│   │   ├── school/                         # وحدة المدرسة (مدير المدرسة)
│   │   │   ├── components/
│   │   │   │   └── SchoolComponents.js     # مكونات عامة لشاشة المدرسة (مثل SchoolDataItem)
│   │   │   ├── screens/
│   │   │   │   ├── ActiveTripsScreen.js    # شاشة عرض الرحلات النشطة
│   │   │   │   ├── SchoolScreen.js         # الشاشة الرئيسية لمدير المدرسة (تم تحديثها)
│   │   │   │   ├── SetSchoolLocationScreen.js # شاشة تحديد موقع المدرسة
│   │   │   │   └── TripMapScreen.js        # شاشة تتبع رحلة معينة على الخريطة
│   │   │   └── services/
│   │   │       ├── exportService.js          # خدمة تصدير البيانات (Excel/PDF)
│   │   │       ├── schoolDataService.js      # خدمة إدارة بيانات المدرسة (CRUD)
│   │   │       └── tripService.js            # خدمة مراقبة الرحلات
│   │   └── staff/                          # وحدة المرافقة
│   │       ├── screens/
│   │       │   └── StaffScreen.js          # الشاشة الرئيسية للمرافقة (تحضير، مفقودات)
│   │       └── services/
│   │           └── staffAttendanceService.js # خدمة إدارة حضور الطلاب للمرافقة
│   ├── services/                           # خدمات عامة مشتركة بين الوحدات
│   │   ├── busService.js                   # خدمة تحديث موقع الباص في Firebase
│   │   ├── locationService.js              # خدمة أذونات الموقع وجلب الموقع الحالي
│   │   └── sessionService.js               # خدمة إدارة جلسة المستخدم (AsyncStorage)
│   └── utils/                              # أدوات مساعدة عامة
│       ├── auth.js                         # دالة تشفير كلمات المرور (hashPassword) (غير مستخدمة حالياً)
│       ├── geo.js                          # دوال جغرافية (مثل حساب المسافة)
│       └── notifications.js                # دوال إشعارات Expo (تسجيل، إرسال)
├── App.js                                  # نقطة الدخول الرئيسية للتطبيق (إعدادات التنقل)
├── index.js                                # نقطة دخول Expo
├── package.json                            # تعريف المشروع والتبعيات
└── ... (ملفات أخرى مثل .gitignore، app.json)
```

## 3. شرح الوحدات الرئيسية ومنطق العمل 🧠

### 3.1. نقطة الدخول والتنقل (`App.js`, `index.js`)

*   **`index.js`**: هو الملف الذي يبدأ تشغيل تطبيق Expo. يقوم بتسجيل المكون الرئيسي `App.js`.
*   **`App.js`**: يدير هيكل التنقل (Navigation) باستخدام `@react-navigation/stack`. يحدد الشاشات المتاحة في التطبيق (Login, SuperAdmin, School, Driver, Staff, Parent, ActiveTrips, TripMap, SetSchoolLocation) ويتحكم في تدفق المستخدم بينها بناءً على دوره.

### 3.2. وحدة المصادقة (`auth`)

*   **`LoginScreen.js`**: واجهة المستخدم لتسجيل الدخول. تتعامل مع إدخال اسم المستخدم وكلمة المرور، وتدعم خيار "تذكرني"، وتبديل اللغة. تستدعي `authService.js` للتحقق من بيانات الاعتماد.
*   **`authService.js`**: يحتوي على منطق التحقق من تسجيل الدخول (`loginUser`). يتفاعل مع Firebase Realtime Database للتحقق من:
    *   حساب المدير العام (Super Admin).
    *   فهرس المستخدمين (`userIndex`) لتحديد `schoolId` ودور المستخدم.
    *   بيانات المستخدم في المسار الخاص بدوره (مثل `schools/{schoolId}/drivers/{username}`).
    *   حالة اشتراك المدرسة (موقوفة، منتهية).

### 3.3. وحدة المدير العام (`admin`)

*   **`SuperAdminScreen.js`**: الشاشة الوحيدة للمدير العام. تسمح بإدارة المدارس (إضافة، تعديل، حذف، إيقاف/تفعيل)، وتحديد خطط الاشتراك، وإرسال الرسائل للمدارس. تتفاعل مباشرة مع Firebase Realtime Database لإدارة بيانات المدارس والمستخدمين.

### 3.4. وحدة المدرسة (`school`)

*   **`SchoolScreen.js`**: لوحة التحكم الرئيسية لمدير المدرسة. توفر واجهة شاملة لإدارة:
    *   **السائقين، الطلاب، أولياء الأمور، والمرافقين:** إضافة، تعديل، حذف، وعرض تفاصيلهم.
    *   **الإعلانات والرسائل:** إرسال إعلانات جماعية، واستقبال والرد على رسائل من الإدارة العامة أو المستخدمين.
    *   **التقارير والطوارئ:** عرض تقارير الحضور، وحالات الطوارئ المبلغ عنها.
    *   **حدود الباقة:** تظهر حدود عدد الباصات والطلاب المسموح بها بناءً على خطة الاشتراك.
*   **`ActiveTripsScreen.js`**: تعرض قائمة بالرحلات النشطة حالياً في المدرسة، مع إمكانية عرض تفاصيل كل رحلة على الخريطة.
*   **`TripMapScreen.js`**: تعرض خريطة تفاعلية لتتبع حافلة معينة في رحلة نشطة، مع عرض موقعها الحالي وسرعتها.
*   **`SetSchoolLocationScreen.js`**: تسمح لمدير المدرسة بتحديد الموقع الجغرافي للمدرسة على الخريطة، وهو أمر حيوي لحساب المسافات وتنبيهات ولي الأمر.
*   **`schoolDataService.js`**: خدمة مركزية لإدارة بيانات المدرسة في Firebase (CRUD Operations) لمختلف الكيانات (سائقين، طلاب، إلخ).
*   **`tripService.js`**: خدمة لمراقبة الرحلات النشطة وجلب تفاصيل رحلة معينة من Firebase.
*   **`exportService.js`**: خدمة لتصدير بيانات المدرسة إلى ملفات Excel أو PDF.

### 3.5. وحدة السائق (`driver`)

*   **`DriverScreen.js`**: الشاشة الرئيسية للسائق. تعرض معلومات الرحلة، قائمة الطلاب، خريطة تتبع الموقع، وقسم للمفقودات والمعثورات. تسمح للسائق ببدء وإنهاء الرحلة، وتتبع موقعه.
*   **`DriverMap.js`**: مكون الخريطة المستخدم في شاشة السائق لعرض موقعه الحالي.
*   **`driverBackgroundService.js`**: خدمة أساسية لتتبع موقع الحافلة في الخلفية باستخدام Expo TaskManager، مما يضمن استمرار التتبع حتى لو كان التطبيق مغلقاً.
*   **`driverLocationService.js`**: خدمة لتتبع الموقع المباشر (Foreground) للسائق.
*   **`driverStudentService.js`**: خدمة لجلب قائمة الطلاب المرتبطين بسائق معين.

### 3.6. وحدة ولي الأمر (`parent`)

*   **`ParentScreen.js`**: الشاشة الرئيسية لولي الأمر. تعرض خريطة لتتبع موقع حافلة ابنه/ابنته، حالة الطالب (حاضر/غائب)، وتنبيهات وصول الباص. كما توفر قسماً للمفقودات والمعثورات.
*   **`ParentMap.js`**: مكون الخريطة المستخدم في شاشة ولي الأمر لعرض موقع الباص، المدرسة، وموقع ولي الأمر.
*   **`parentAlertService.js`**: خدمة لحساب المسافة بين الباص وولي الأمر وإطلاق تنبيهات وصول الباص.
*   **`parentBusService.js`**: خدمة لجلب معلومات الباص والسائق المرتبطين بطالب معين.
*   **`parentLocationService.js`**: خدمة لجلب الموقع الحالي لولي الأمر.
*   **`parentStudentService.js`**: خدمة لجلب معلومات الطالب المرتبط بولي الأمر وتحديث حالته (حاضر/غائب).

### 3.7. وحدة المرافقة (`staff`)

*   **`StaffScreen.js`**: الشاشة الرئيسية للمرافقة. تسمح للمرافقة بتحضير الطلاب (تحديث حالة الحضور)، وعرض قسم المفقودات والمعثورات.
*   **`staffAttendanceService.js`**: خدمة لإدارة حضور الطلاب وتحديث حالتهم، بالإضافة إلى جلب رقم هاتف ولي الأمر.

### 3.8. وحدة المفقودات والمعثورات (`lostAndFound`)

*   **`lostAndFoundService.js`**: خدمة شاملة لإدارة عناصر المفقودات والمعثورات (إضافة، تحديث، حل، بحث، جلب إحصائيات).
*   **`notificationService.js`**: خدمة مخصصة لإرسال إشعارات الدفع (Push Notifications) المتعلقة بالمفقودات والمعثورات إلى الأطراف المعنية (أولياء الأمور، السائقين، المرافقين).

### 3.9. الخدمات والأدوات المساعدة المشتركة (`services`, `utils`, `i18n`, `firebaseConfig`)

*   **`firebaseConfig.js`**: يحتوي على إعدادات الاتصال بـ Firebase Realtime Database و Firebase Storage. يتم استيراده في جميع الخدمات التي تتفاعل مع Firebase.
*   **`busService.js`**: دالة عامة لتحديث موقع الباص وسرعته في Firebase. تستخدمها خدمات السائق لتحديث الموقع.
*   **`locationService.js`**: دوال عامة لطلب أذونات الموقع (Foreground/Background) وجلب الموقع الحالي للجهاز.
*   **`sessionService.js`**: دوال لإدارة جلسة المستخدم (حفظ، جلب، مسح) باستخدام `AsyncStorage`.
*   **`geo.js`**: يحتوي على دوال جغرافية مثل `calculateDistance` لحساب المسافة بين نقطتين.
*   **`notifications.js`**: دوال عامة لتسجيل الجهاز لاستقبال إشعارات Expo وإرسال إشعارات محلية.
*   **`i18n.js`**: ملف يحتوي على كائنات الترجمة (Translations) للغة العربية والإنجليزية، مما يسهل دعم اللغات المتعددة في التطبيق.
*   **`auth.js`**: يحتوي على دالة `hashPassword` لتشفير كلمات المرور. **ملاحظة: هذه الدالة غير مستخدمة حالياً في منطق المصادقة ويجب تفعيلها لأسباب أمنية.**
*   **`useFirebase.js`**: يحتوي على `useFirebaseList` وهو React Hook مخصص لجلب قوائم البيانات من Firebase. **ملاحظة: هذا الـ Hook غير مستخدم حالياً في أي مكان في المشروع.**

## 4. هيكل قاعدة البيانات (Firebase Realtime Database Schema) ☁️

يعتمد التطبيق على Firebase Realtime Database لتخزين جميع بياناته. إليك الهيكل العام للبيانات:

*   **`admin_settings/`**
    *   `super_admin/`: معلومات المدير العام (اسم المستخدم، كلمة المرور).
*   **`userIndex/`**
    *   `{username_safe_key}/`: فهرس سريع للمستخدمين، يحتوي على `schoolId` ودور المستخدم (`role`). يتم استخدام `.` في اسم المستخدم كـ `,` لتجنب مشاكل المسارات في Firebase.
*   **`users/`**
    *   `{username_safe_key}/`: بيانات المستخدمين الأساسية (اسم المستخدم، كلمة المرور، الدور، الاسم، `schoolId`). يستخدم للمدراء الرئيسيين للمدارس.
*   **`schools/`**
    *   `{schoolId}/`: كل مدرسة لها عقدة خاصة بها تحتوي على:
        *   `name`: اسم المدرسة.
        *   `displayName`: الاسم المعروض للمدرسة.
        *   `logoUrl`: رابط شعار المدرسة.
        *   `googleMapsLink`: رابط جوجل ماب للمدرسة.
        *   `planType`: نوع خطة الاشتراك (أساسية، متقدمة، احترافية).
        *   `email`: بريد مدير المدرسة.
        *   `password`: كلمة مرور مدير المدرسة (يجب تشفيرها).
        *   `startDate`, `endDate`: تواريخ بدء وانتهاء الاشتراك.
        *   `status`: حالة الاشتراك (active, suspended).
        *   `location`: إحداثيات موقع المدرسة (`latitude`, `longitude`).
        *   `socialLinks`: روابط التواصل الاجتماعي للمدرسة.
        *   `limits`: حدود الباقة (maxBuses, maxStudents) (يجب حفظها هنا عند إنشاء المدرسة).
        *   `drivers/`: قائمة السائقين التابعين للمدرسة.
            *   `{driverUsername}/`: بيانات السائق (اسم، رقم هاتف، رقم باص، إلخ).
        *   `students/`: قائمة الطلاب التابعين للمدرسة.
            *   `{studentId}/`: بيانات الطالب (اسم، صف، ولي أمر، سائق، حالة).
        *   `parents/`: قائمة أولياء الأمور التابعين للمدرسة.
            *   `{parentUsername}/`: بيانات ولي الأمر (اسم، رقم هاتف، إلخ).
        *   `staff/`: قائمة المرافقين التابعين للمدرسة.
            *   `{staffUsername}/`: بيانات المرافقة (اسم، رقم هاتف، سائق).
        *   `bus/`: معلومات الباصات الحالية (تستخدم لتتبع الموقع).
            *   `{driverUsername}/`: موقع الباص الحالي للسائق (latitude, longitude, speed, isActive, updatedAt).
        *   `managers/`: قائمة المدراء الفرعيين للمدرسة.
            *   `{managerUsername}/`: بيانات المدير الفرعي وصلاحياته.
        *   `emergencies/`: قائمة حالات الطوارئ المبلغ عنها.
        *   `reports/`: تقارير مختلفة (مثل تقارير الحضور).
        *   `messages/`: رسائل داخلية بين مدير المدرسة والمستخدمين.
        *   `lostAndFound/`: عناصر المفقودات والمعثورات.
*   **`trips/`**
    *   `{schoolId}/`: الرحلات النشطة حالياً في كل مدرسة.
        *   `{tripId}/`: تفاصيل الرحلة (driverName, busNumber, startTime, status, location).
*   **`admin_inbox/`**
    *   `{schoolId}/`: صندوق الوارد الخاص بالمدير العام لكل مدرسة (رسائل من المدارس).

## 5. تدفق البيانات الجغرافية والتتبع 🗺️

يعد تتبع الموقع جزءاً أساسياً من التطبيق، ويتم التعامل معه كالتالي:

1.  **أذونات الموقع:** يتم طلب أذونات الموقع (Foreground و Background) من المستخدم عند الحاجة، خاصة للسائقين.
2.  **تتبع السائق:**
    *   عند بدء الرحلة من `DriverScreen.js`، يتم تفعيل `driverBackgroundService.js`.
    *   تستخدم هذه الخدمة `Expo TaskManager` لتتبع الموقع في الخلفية بشكل مستمر.
    *   يتم تحديث موقع الباص وسرعته في Firebase (`schools/{schoolId}/bus/{driverUsername}`) باستخدام `busService.js`.
3.  **تتبع ولي الأمر:**
    *   `ParentScreen.js` يشترك في تحديثات موقع الباص من Firebase (`schools/{schoolId}/bus/{driverUsername}`).
    *   يتم جلب موقع ولي الأمر الحالي باستخدام `parentLocationService.js`.
    *   `parentAlertService.js` يحسب المسافة بين الباص وولي الأمر ويطلق تنبيهات عند اقتراب الباص.
4.  **تتبع مدير المدرسة:**
    *   `ActiveTripsScreen.js` تعرض الرحلات النشطة بناءً على بيانات `trips/{schoolId}`.
    *   `TripMapScreen.js` تعرض موقع الباص الحي لرحلة محددة من `trips/{schoolId}/{tripId}`.

## 6. ملاحظات هامة وتوصيات أمنية 🔒

*   **تشفير كلمات المرور:** يجب تفعيل دالة `hashPassword` في `src/utils/auth.js` واستخدامها لتشفير جميع كلمات المرور قبل حفظها في Firebase، وكذلك عند التحقق من تسجيل الدخول. هذا أمر بالغ الأهمية للأمان.
*   **مفتاح Google Maps API:** يجب إضافة مفتاح Google Maps API في `app.json` لضمان عمل الخرائط بشكل صحيح في بيئة الإنتاج وتجنب ظهور العلامة المائية.
*   **تنظيف الكود:** إزالة الملفات والدوال غير المستخدمة (مثل `useFirebase.js` و `driverTripService.js` و `src/utils/auth.js` إذا لم يتم تفعيلها) لتقليل حجم الكود وتحسين الأداء.
*   **إدارة الحالة (State Management):** مراجعة الـ `useEffect` Dependencies في الشاشات المعقدة مثل `ParentScreen.js` و `DriverScreen.js` لتجنب إعادة الاشتراك غير الضرورية أو تسرب الذاكرة.
*   **حدود الباقة:** التأكد من حفظ `limits` (maxBuses, maxStudents) في Firebase عند إنشاء المدرسة في `SuperAdminScreen.js` لضمان تطبيقها بشكل صحيح في `SchoolScreen.js`.
*   **التعامل مع الأخطاء:** التأكد من وجود معالجة شاملة للأخطاء في جميع الخدمات والشاشات لتقديم تجربة مستخدم مستقرة.

## 7. الخلاصة

يمثل مشروع SchoolBusApp قاعدة قوية لتطبيق تتبع حافلات المدارس. من خلال هذه الوثيقة، يمكن لأي مطور أو نظام ذكاء اصطناعي فهم بنيته المعقدة، آليات عمله، وتحديد نقاط التحسين المحتملة. الالتزام بالتوصيات الأمنية وتحسينات الأداء سيضمن استمرارية ونجاح المشروع على المدى الطويل.

--- 

**المؤلف:** Manus AI
**التاريخ:** 1 يونيو 2026
