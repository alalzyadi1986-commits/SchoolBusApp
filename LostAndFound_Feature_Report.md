# تقرير ميزة "لوحة المفقودات الذكية" (Smart Lost & Found)

## 1. المقدمة

تم تطوير ميزة "لوحة المفقودات الذكية" لتطبيق SchoolBusApp بهدف حل مشكلة ضياع الأغراض الشخصية للطلاب في حافلات المدارس. توفر هذه الميزة نظامًا متكاملًا يربط بين المرافقة، الأهل، الإدارة، والسائق لتبسيط عملية الإبلاغ عن المفقودات والعثور عليها وإرجاعها.

## 2. المشكلة التي تعالجها الميزة

كانت عملية التعامل مع الأغراض المفقودة تستغرق وقتًا طويلاً وتفتقر إلى التنظيم، حيث كان الأهل يتصلون بالمدرسة، والمدرسة بدورها تتواصل مع السائق، مما يؤدي إلى ضياع الوقت وصعوبة تتبع الأغراض. تهدف هذه الميزة إلى توفير قناة اتصال مباشرة وفعالة بين جميع الأطراف المعنية.

## 3. الحل الإبداعي: لوحة المفقودات الذكية

تعتمد الميزة على مفهوم لوحة إعلانات رقمية تفاعلية تتيح للمرافقة الإبلاغ عن الأغراض المعثور عليها، ولولي الأمر التبليغ عن الأغراض المفقودة، مع إشعارات موجهة لكل طرف لضمان سرعة الاستجابة.

## 4. التعديلات المنفذة

تم إجراء تعديلات شاملة على هيكل البيانات والخدمات وواجهات المستخدم لدمج هذه الميزة الجديدة:

### 4.1. هيكل بيانات Firebase (ملف `docs/LostAndFound_Firebase_Schema.md`)

تم تحديث هيكل البيانات في Firebase Realtime Database ليشمل مسارًا جديدًا `lostAndFound` داخل كل `schoolId`. هذا المسار يدعم نوعين من الإدخالات:

*   **`type: "found"`**: للعناصر التي تعثر عليها المرافقة أو الإدارة في الباص.
*   **`type: "lost"`**: للعناصر التي يبلغ عنها ولي الأمر.

تم إضافة حقول جديدة لتتبع حالة العنصر (`status: "active"` أو `"resolved"`)، ومعلومات المبلغ أو المكتشف، ومعلومات المطالب، وتفاصيل التسليم والتأكيد من الطرفين. يضمن هذا الهيكل المرونة والشمولية في تتبع كل عنصر.

### 4.2. خدمة المفقودات والمعثورات (ملف `src/modules/lostAndFound/services/lostAndFoundService.js`)

تم تحديث هذه الخدمة لتشمل الدوال التالية:

*   **`addFoundItem(schoolId, itemData, itemImageURL)`**: لإضافة عنصر معثور عليه بواسطة المرافقة/الإدارة.
*   **`reportLostItem(schoolId, itemData, itemImageURL)`**: لتمكين ولي الأمر من التبليغ عن عنصر مفقود.
*   **`uploadLostItemImage(schoolId, lostItemId, imageData)`**: لرفع صور الأغراض المفقودة/المعثور عليها إلى Firebase Storage.
*   **`subscribeLostAndFoundItems(schoolId, callback)`**: للاشتراك في جميع العناصر المفقودة والمعثور عليها لمدرسة معينة.
*   **`subscribeFoundItemsForParent(schoolId, busId, callback)`**: لعرض العناصر المعثور عليها في باص معين لولي الأمر.
*   **`subscribeLostItemsByParent(schoolId, parentUsername, callback)`**: لعرض بلاغات ولي الأمر عن المفقودات.
*   **`claimFoundItem(schoolId, lostItemId, parentUsername, parentName)`**: لتمكين ولي الأمر من المطالبة بعنصر معثور عليه.
*   **`resolveLostAndFoundItem(schoolId, lostItemId, staffUsername, returnedToParentUsername)`**: لتحديث حالة العنصر إلى "تم الحل" بعد التسليم.
*   **`confirmParentReceipt(schoolId, lostItemId)`**: لتأكيد ولي الأمر استلام العنصر.
*   **`deleteLostAndFoundItem(schoolId, lostItemId)`**: لحذف عنصر من النظام (صلاحية الإدارة).

### 4.3. مكونات الواجهة المشتركة (ملف `src/modules/lostAndFound/components/LostAndFoundComponents.js`)

تم تطوير مكونات React Native قابلة لإعادة الاستخدام:

*   **`LostAndFoundItemCard`**: بطاقة عرض موحدة للعناصر المفقودة/المعثور عليها، تعرض التفاصيل الأساسية، الصورة، الحالة، وتوفر أزرار الإجراءات المناسبة لكل دور (المطالبة، الحل، التأكيد، الحذف).
*   **`AddFoundItemForm`**: نموذج لإضافة عنصر معثور عليه، يستخدمه المرافقة/الإدارة.
*   **`ReportLostItemForm`**: نموذج للإبلاغ عن عنصر مفقود، يستخدمه ولي الأمر.

### 4.4. تحديث واجهة المرافقة (ملف `src/modules/staff/screens/StaffScreen.js`)

تم إضافة تبويب جديد باسم "المفقودات الذكية" إلى شاشة المرافقة. يمكن للمرافقة الآن:

*   عرض قائمة بالعناصر المعثور عليها في باصها.
*   عرض بلاغات الأهل عن المفقودات المتعلقة بباصها.
*   إضافة عنصر معثور عليه جديد (مع صورة ووصف وتحديد الباص).
*   تحديث حالة العناصر إلى "تم الحل" بعد تسليمها.

### 4.5. تحديث واجهة ولي الأمر (ملف `src/modules/parent/screens/ParentScreen.js`)

تم إضافة تبويب جديد باسم "المفقودات الذكية" إلى شاشة ولي الأمر. يمكن لولي الأمر الآن:

*   عرض قائمة بالعناصر المعثور عليها في باص ابنه فقط.
*   المطالبة بعنصر معثور عليه بضغطة زر.
*   التبليغ عن عنصر مفقود لابنه (مع صورة ووصف وتحديد الطالب).
*   تأكيد استلام العنصر بعد حله.

### 4.6. تحديث واجهة الإدارة (ملف `src/modules/school/screens/SchoolScreen.js`)

تم إضافة تبويب جديد باسم "المفقودات والمعثورات" إلى شاشة الإدارة. يمكن للمدير الآن:

*   عرض جميع العناصر المفقودة والمعثور عليها في المدرسة.
*   تصفية العناصر حسب النوع، الحالة، أو الباص.
*   حل أي عنصر أو حذفه بشكل نهائي.
*   إدارة صلاحيات الموظفين للتعامل مع المفقودات.

### 4.7. تحديث واجهة السائق (ملف `src/modules/driver/screens/DriverScreen.js`)

تم إضافة تبويب جديد باسم "مفقودات الباص" إلى شاشة السائق. يمكن للسائق الآن:

*   عرض قائمة بالعناصر المفقودة والمعثور عليها المرتبطة بباصه.
*   تلقي إشعارات حول المفقودات المبلغ عنها أو المطالبات بالعناصر المعثور عليها في باصه.

### 4.8. خدمة الإشعارات (ملف `src/modules/lostAndFound/services/notificationService.js`)

تم إنشاء خدمة إشعارات جديدة باستخدام `expo-notifications` لضمان تدفق المعلومات بين الأطراف:

*   **`notifyParentsOfFoundItem`**: إرسال إشعار للأهل عند العثور على غرض في باص أبنائهم.
*   **`notifyStaffAndDriverOfLostReport`**: إرسال إشعار للمرافقة والسائق عند تبليغ ولي أمر عن مفقود.
*   **`notifyStaffAndDriverOfClaim`**: إرسال إشعار للمرافقة والسائق عند مطالبة ولي أمر بعنصر معثور عليه.
*   **`notifyParentOfItemResolved`**: إرسال إشعار لولي الأمر عند حل مشكلة المفقود/المعثور عليه.
*   **`sendPushNotification`**: دالة عامة لإرسال إشعارات Expo.
*   **`logNotification`**: لتسجيل الإشعارات المرسلة في Firebase للمتابعة.

## 5. الخلاصة

تعتبر ميزة "لوحة المفقودات الذكية" إضافة قيمة لتطبيق SchoolBusApp، حيث توفر حلاً فعالاً ومنظمًا لمشكلة الأغراض المفقودة. بفضل التكامل بين هيكل البيانات، الخدمات، وواجهات المستخدم، والإشعارات الموجهة، سيتمكن جميع المستخدمين من التفاعل بسلاسة مع هذه الميزة، مما يعزز من تجربة المستخدم ويوفر الوقت والجهد على جميع الأطراف.
