# تقرير فحص وتحليل مشروع SchoolBusApp 🚌

## 1. نظرة عامة على المشروع 🌟
تم فحص مشروع **SchoolBusApp** وهو تطبيق متكامل مبني باستخدام React Native و Expo، يهدف إلى تتبع حافلات المدارس وإدارة العمليات المتعلقة بها. التطبيق مقسم بشكل جيد إلى وحدات (Modules) بناءً على دور المستخدم (سوبر أدمن، مدرسة، سائق، مرافقة، ولي أمر). يعتمد المشروع على Firebase Realtime Database كقاعدة بيانات أساسية، و Expo لخدمات الموقع والإشعارات.

## 2. البنية المعمارية للمشروع 🏗️
يعتمد المشروع على بنية معمارية تعتمد على الوحدات (Modular Architecture) مما يسهل عملية الصيانة والتطوير. تم رسم مخططين لتوضيح البنية:
- **المخطط التفصيلي (project_diagram.png):** يوضح تدفق البيانات والتبعيات بين جميع الملفات والخدمات.
- **المخطط الهرمي (project_structure.png):** يوضح البنية العامة للتطبيق وكيفية اتصال الشاشات بقاعدة البيانات عبر الخدمات.

*ملاحظة: المخططات مرفقة مع هذا التقرير كصور بصيغة PNG.*

## 3. الأخطاء البرمجية والمشكلات المكتشفة 🐞
بعد تحليل الكود بعمق، تم اكتشاف مجموعة من الأخطاء البرمجية والمشكلات التي يجب معالجتها لضمان استقرار التطبيق وأمانه:

### أ. أخطاء حرجة (Critical) 🚨
1. **تخزين كلمات المرور بنص واضح (Plain Text):**
   - في ملف `SuperAdminScreen.js` و `authService.js` يتم تخزين والتحقق من كلمات المرور كنص واضح.
   - **المشكلة:** هذا يشكل خطراً أمنياً كبيراً.
   - **الحل:** يجب استخدام دالة التشفير الموجودة مسبقاً `hashPassword` في ملف `src/utils/auth.js` (والتي لا يتم استخدامها حالياً) لتشفير كلمات المرور قبل حفظها وقبل التحقق منها.

2. **تسرب الذاكرة في `ParentScreen.js` (Memory Leak):**
   - استخدام `requestAnimationFrame` داخل دالة `animateBus` بشكل متكرر دون تنظيف كافٍ في بعض الحالات.
   - **المشكلة:** يؤدي إلى استهلاك عالي للبطارية وتهنيج التطبيق عند تركه مفتوحاً لفترة طويلة.
   - **الحل:** التأكد من استدعاء `cancelAnimationFrame(animationFrame.current)` في دالة التنظيف (cleanup) الخاصة بـ `useEffect` عند إغلاق الشاشة.

3. **استخدام مكتبة خرائط Google بدون مفتاح API:**
   - تم استخدام `PROVIDER_GOOGLE` في شاشتي `SetSchoolLocationScreen.js` و `TripMapScreen.js`.
   - **المشكلة:** لم يتم إعداد `googleMapsApiKey` في ملف `app.json`، مما سيؤدي إلى ظهور علامة مائية على الخريطة أو تعطلها في بيئة الإنتاج (Production).
   - **الحل:** إضافة مفتاح API الخاص بخرائط جوجل في `app.json` تحت قسم `android.config.googleMaps.apiKey` و `ios.config.googleMapsApiKey`.

### ب. أخطاء متوسطة (Medium) ⚠️
1. **مشكلة `animateCamera` في `DriverScreen.js`:**
   - تم استخدام `mapRef.current?.animateCamera({ center: location, zoom: 17 })` في السطر 114.
   - **المشكلة:** هذه الدالة قد تسبب مشاكل مع بعض إصدارات `react-native-maps` وتفضل استخدام `animateToRegion`.
   - **الحل:** استبدالها بـ `animateToRegion` مع حساب `latitudeDelta` و `longitudeDelta` المناسبين.

2. **الاعتماديات المفقودة أو القديمة:**
   - استخدام `expo-file-system/legacy` في `exportService.js`، وهو مسار قديم (Legacy).
   - استيراد `Ionicons` من `@expo/vector-icons` ولكن المكتبة غير موجودة في `package.json`.

3. **تكرار الخدمات (Code Duplication):**
   - هناك تكرار في بعض الخدمات مثل `startDriverTrip` و `stopDriverTrip` الموجودة في `driverTripService.js` ولكن يتم استخدام `startBackgroundTracking` و `stopBackgroundTracking` بدلاً منها في `DriverScreen.js`.

### ج. أخطاء طفيفة وملاحظات تحسينية (Minor/Enhancements) 💡
1. **ملفات غير مستخدمة (Dead Code):**
   - `src/utils/auth.js`: دالة `hashPassword` غير مستخدمة.
   - `src/hooks/useFirebase.js`: الـ Hook `useFirebaseList` غير مستخدم في أي مكان.
   - `SchoolScreen.js.backup`: ملف نسخة احتياطية يجب إزالته من بيئة الإنتاج.

2. **مشكلة `limits` في `SuperAdminScreen.js`:**
   - لا يتم حفظ حدود الباقة (`limits`) في Firebase عند إنشاء مدرسة جديدة، بل يتم الاعتماد على الدالة المحلية `getPlanLimits`. في `SchoolScreen.js` يتم محاولة قراءة `data.limits` والتي قد تكون غير موجودة.

3. **إدارة الحالة (State Management) في `ParentScreen.js`:**
   - تم استخدام `studentInfo` كـ Dependency في `useEffect` الثاني، مما قد يؤدي إلى إعادة اشتراك متكرر غير ضروري إذا تغيرت حالة الطالب بشكل طفيف.

## 4. التوصيات والخطوات القادمة 🎯
بناءً على التحليل، أنصح بالقيام بالخطوات التالية بالترتيب:
1. **الأمان أولاً:** تفعيل تشفير كلمات المرور باستخدام `hashPassword`.
2. **إصلاح الخرائط:** إضافة مفتاح Google Maps API وتعديل `animateCamera` إلى `animateToRegion`.
3. **تحسين الأداء:** مراجعة الـ Dependencies في `useEffect` خاصة في `ParentScreen` و `DriverScreen`، والتأكد من إلغاء الاشتراكات (Unsubscribe) وتنظيف الـ Animations.
4. **تنظيف الكود:** حذف الملفات غير المستخدمة والنسخ الاحتياطية.

## 5. الأسئلة والاستفسارات 💬
بما أنك ذكرت أنك ستقوم بسؤالي عن الأكواد وبعض التعديلات، أنا الآن جاهز تماماً للإجابة على أي سؤال. يمكنك سؤالي عن:
- كيفية تطبيق التشفير في صفحة تسجيل الدخول.
- كيفية إصلاح مشكلة الخرائط.
- كيفية تحسين أداء التتبع المباشر.
- أو أي تعديل آخر ترغب في إضافته للمشروع.
